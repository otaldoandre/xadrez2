Contém as 64 casas que o compõem, bem como as peças e suas posições.

Deve conter:
`boolean noLimite(linha, coluna); // dispensável`
`String desenho(); // representação do tabuleiro. substituido na versão gráfica`
