Classe abstrata que contém a interface para os métodos:
`String desenho();`
`boolean movimentoValido();`
`Caminho caminho(); // como isso funcionaria nessa interface de texto?`
existentes nas variantes específicas.